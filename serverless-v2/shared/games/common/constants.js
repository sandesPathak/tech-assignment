'use strict';

// Compat shim — engine moved to @hijack/engine in Phase 0.
// Kept so the symlinked `shared/` path keeps working for existing serverless services.
module.exports = require('@hijack/engine/constants');
