{
  "name": "@hijack/protocol",
  "version": "0.1.0",
  "private": true,
  "description": "Shared WS / REST message types for Hijack Poker (web, gateway, worker, bot)",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "echo \"no protocol-package tests yet\" && exit 0",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
