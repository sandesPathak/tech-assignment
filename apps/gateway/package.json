{
  "name": "@hijack/gateway",
  "version": "0.1.0",
  "private": true,
  "description": "Hijack Poker WebSocket gateway — fan-out, auth, reconnection (Fly.io).",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node src/index.js",
    "test": "jest"
  },
  "dependencies": {
    "@hijack/engine": "*",
    "@hijack/observability": "*",
    "@hijack/protocol": "*",
    "ioredis": "^5.4.1",
    "jsonwebtoken": "^9.0.2",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@hijack/worker": "*",
    "ioredis-mock": "^8.9.0",
    "jest": "^29.7.0"
  },
  "jest": {
    "testEnvironment": "node",
    "testMatch": ["**/__tests__/**/*.test.js"],
    "testTimeout": 15000
  }
}
